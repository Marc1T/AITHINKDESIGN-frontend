# =============================================================================
# Generative Designer API Configuration
# =============================================================================
# URL du backend API generative-designer
# Modifier selon votre environnement (dev/prod)
NEXT_PUBLIC_GENERATIVE_DESIGNER_API_URL=http://localhost:8000/api/generative-designer

# Supabase (already configured in MakerKit)
# NEXT_PUBLIC_SUPABASE_URL=...
# NEXT_PUBLIC_SUPABASE_ANON_KEY=...

# =============================================================================
# Existing MakerKit Configuration (à conserver)
# =============================================================================
# Voir apps/web/.env.local.example ou apps/web/.env.local existant
# pour les autres variables (Supabase, etc.)
